# Evrenkent — UI Restyle Notları

Bu dosya, `dosyalar/` klasöründeki mockup'lara görsel yakınsama çalışmasının
neden/nasıl/ne zaman kayıtlarını tutar. Amaç: ileride "bu neden böyle
yapılmış" ya da "bunu değiştirmiş miydik" karışıklığını önlemek.

Fonksiyonel/işlevsel durum (route/controller/policy/test) bu dosyanın konusu
**değil** — o taraf zaten sağlam ve ayrıca doğrulandı (82 test geçiyor).
Burada sadece **görsel/tasarım** kararları ve gerekçeleri var.

## Kapsam dışı bırakılanlar (bilinçli, sonraya ertelendi)

Bunlara bu faz içinde **dokunmuyoruz** — karıştırmayalım:

- **Kitap kapak görselleri** (`x-book-cover` bileşeni, gerçek illüstrasyon/upload) — placeholder gradyan aynen kalıyor.
- **Okuma modu sayfası** (`books/read.blade.php`) — şu an sade, mockup'taki "kitap sayfası" hissi (kağıt dokusu, gömülü belge/video) sonraya.
- ~~Dergi Editörü / Süper Admin panelleri generic Filament admin arayüzü kullanıyor~~ — **Dergi Editörü** tarafı artık gerçek bir dashboard'a kavuştu (madde 24) ve kendi günlük işlemlerini (sayı oluştur/düzenle/onaya gönder, makale görüntüle/incele) hiç Filament'e girmeden kendi panelinde yapabiliyor (madde 25) — Filament'te kalan tek şey Süper Admin'in onayla/reddet/yayınla aksiyonları (zaten policy'de sadece ona açık). **Süper Admin** tarafı da artık kendi dashboard'una kavuştu (madde 26). Onay/red/yayınla ve kullanıcı/rol yönetimi aksiyonları hâlâ Filament'te kalıyor — bilinçli bir tercih, zaten çalışıyor ve iki yerde tekrarlamaya değmiyor.
- **Süper Admin'in Gelir Merkezi / Premium Sistemi / Abonelikler / Sözlükler / Platform Sağlığı** — mockup'ta var ama hiç altyapısı yok (komisyon/payı alanı, abonelik sistemi, sözlük veri modeli, sunucu/depolama izleme). Madde 26'da tek bir dürüst "yakında, altyapı hazırlanıyor" placeholder sayfasına yönlendiriliyor — sahte veri/işlevsellik üretilmedi.

## Bilinçli olarak mockup'tan FARKLI bırakılanlar (ve nedeni)

- ~~Header'daki arama ikonu ve sepet ikonu sadece görsel.~~ **Artık gerçek**
  (bkz. madde 20/21) — arama Kitap+Dergi+Makale üzerinde gerçek bir sorgu
  çalıştırıyor, sepet çoklu ürün + gerçek (mock ödemeli) checkout akışına
  sahip. Bu madde tarihsel referans olarak bırakıldı.
- **Hamburger menü (☰), girişli kullanıcı için gerçek işlevsel** —
  `auth()->user()->redirectPath()`'e gidiyor, "Panelim" text linkinin
  yerini aldı. Ziyaretçi için henüz bir mega-menü/kategori gezinme paneli
  yok, o yüzden ziyaretçide görsel/pasif (`title="Yakında"`) kalıyor —
  bu da ileride ayrı bir iş.
- **Anasayfadaki "Çok Satanlar / Editörün Seçkisi / Fırsatlar / Yakında
  Çıkacaklar" sekmeleri pasif/tıklanamaz** (soluk renkte, `title="Yakında"`).
  Bu verileri besleyecek bir model/sıralama mantığı henüz yok; sahte içerik
  göstermek yerine dürüst bir "yakında" hali tercih edildi. Sadece "Yeni
  Çıkanlar" gerçek veriyle çalışıyor (zaten `HomeController` bunu üretiyor).
- **"Sepetim 2" gibi sahte sayaçlar eklenmedi.** Mockup'ta sepette 2 ürün
  gösteriliyor ama bu gerçek bir veri değil — sahte sayı göstermek yanıltıcı
  olur, o yüzden sepet ikonu sayaçsız.
- **Kitap Tanıtım Sayfası'nda yıldız puanı / değerlendirme sayısı / sayfa-
  belge-video-harita sayaç şeridi yok.** Mockup'ta var ama projede hiç bir
  yorum/puanlama (review/rating) sistemi kurulmadı — bu veriyi uydurmak
  (örn. "4.8 yıldız, 128 değerlendirme") kullanıcıyı yanıltır. Onun yerine
  **gerçek veri**yle dolduruldu: kategori etiketleri (`Book::categories`),
  bölüm sayısı (`chapters()->count()`), yayın tarihi. Yorum/puanlama sistemi
  ayrı bir özellik olarak eklenirse bu şerit genişletilebilir.

## İlerleme sırası ve durum

| # | Alan | Durum | Ne yapıldı |
|---|------|-------|------------|
| 1 | Tasarım sistemi (renk/kart/buton) | ✅ Tamamlandı | `tailwind.config.js`'e `brand` turuncu skala; `app.css`'e `.btn-brand/.btn-dark/.btn-outline`, `.card/.card-hover`, `.pill-active/.pill-idle` sınıfları |
| 2 | Header/nav | ✅ Tamamlandı (4 tur kullanıcı geri bildirimiyle revize edildi) | `layouts/public.blade.php` — solda hamburger ikonu, arama artık sadece ikon (kutu değil), sepet ikon+"Sepetim" yazısı (sayaç yok — sahte veri), "Kayıt Ol" butonu `.btn-dark`. **2. revizyon:** Hamburger sayfa değiştirmiyor — girişli kullanıcıda Alpine.js ile soldan açılan bir panel menüsü açıyor. Panel menüsü tekrarını önlemek için `components/panel-nav.blade.php` bileşenine çıkarıldı, hem `layouts/panel.blade.php` hem bu menü aynı bileşeni kullanıyor. **3. revizyon:** İlk halde menü koyu bir perde (backdrop) ile sayfanın üstüne overlay olarak açılıyordu — kullanıcı bunun yerine YouTube tarzı istedi: artık overlay değil, normal doküman akışında; açılınca header hariç içerik alanı (`main`+`footer`) gerçekten daralıyor/itiliyor, backdrop yok, sidebar sticky olarak scroll'da sabit kalıyor. Ziyaretçide hamburger hâlâ görsel/pasif (mega-menü yok). **4. revizyon (2026-08-14):** Üç değişiklik birden: (a) Ziyaretçide hamburger artık hiç render edilmiyor — pasif/soluk ikon yerine tamamen kaldırıldı (mega-menüsü olmayan bir ikonu göstermenin anlamı yoktu). (b) `User::redirectPath()` — okur girişten sonra artık `/panel`'e değil `/`'e (anasayfa) düşüyor, sidebar zaten açık geliyor. (c) **Gerçek SPA-hissi:** `layouts/panel.blade.php` kendi header/sidebar'ını kurmayı bıraktı, `@extends('layouts.public')`'a indirgendi — artık panel sayfaları da aynı ortak kabuğu kullanıyor. `@hotwired/turbo` eklendi (`resources/js/app.js`) — artık sidebar'daki (veya sitedeki herhangi bir) linke tıklamak tam sayfa yenilemesi yapmıyor, Turbo Drive `<body>`'yi fetch ile değiştirip sadece `<main>` içeriğini günceliyor, header/sidebar DOM'da kalıyor (YouTube'daki gibi). Sidebar açık/kapalı durumu artık `Alpine.store('ui').sidebarOpen`'da tutuluyor (`x-data` yerine) — Turbo `<body>`'yi değiştirse de JS ortamı (ve dolayısıyla store) sayfalar arası canlı kaldığı için sidebar durumu geçişler arasında kaybolmuyor. Aktif link vurgulaması (`request()->routeIs()`) her Turbo geçişinde sunucudan taze geldiği için sidebar `data-turbo-permanent` **değil** — bilerek, aksi halde eski sayfanın vurgusu donardı. |
| 3 | Anasayfa | ✅ Tamamlandı (3 tur kullanıcı geri bildirimiyle revize edildi) | `home.blade.php` — kategori sekmeleri (pill), kitap kartları `.card-hover`. "Kitaplar" kutusu ve aktif "Yeni Çıkanlar" pill'i arka plan rengi değiştirmiyor (sadece kenarlık/metin), kutu ikonları daire arka plandan çıkarıldı (düz ikon). **2. revizyon:** border-radius `rounded-2xl` fazla geldi — `.card`/`.card-hover` ve stat kutuları `rounded-lg`'ye küçültülüp eşitlendi. **3. revizyon:** pill'ler (`Yeni Çıkanlar/Çok Satanlar/...`) artık `.card`/stat kutularıyla aynı `rounded-lg` + renk mantığını kullanıyor (`pill` sınıfı `rounded-full`'den `rounded-lg`'ye değişti; idle pilller stat kutularındaki gibi `text-slate-400`). Sayfa üstündeki "Okumanın yeni bir evreni / Kitaplar, dergiler ve sözlükler — tek yerde" başlık bloğu kaldırıldı (gereksiz görüldü). Kitap sayfasındaki gerçek kategori etiketleri (`Roman` vb.) artık ayrı bir `.pill-tag` sınıfı kullanıyor — `pill-idle`'ın "yakında/pasif" soluk rengini miras almasın diye. |
| 4 | Kitap Tanıtım Sayfası | ✅ Tamamlandı | `books/show.blade.php` — sağda sticky "satın alma kartı" (fiyat + CTA yığını), gerçek kategori etiketleri, gerçek bölüm sayısı+yayın tarihi, gerçek verili "Bu Eserler de Dikkatini Çekebilir" şeridi. `BookController::show()` — `categories` eager-load, `chapterCount`, `relatedBooks` (önce kategori eşleşmesi, yetmezse yazarın diğer kitaplarıyla tamamlanır) eklendi |
| 5 | Kitaplığım | ✅ Tamamlandı | `panel/kitapligim.blade.php` — veri tarafı (`bea0f2c`) ayrı bir işti; bu fazda görsel olarak tasarım sistemine geçirildi: ham `bg-white border rounded-lg` yerine `.card`, kategori etiketi `.pill-tag` (küçük varyant, `show.blade.php` ile aynı), aksiyon butonları `.btn-brand`/`.btn-outline` `btn-sm`, "Favori" rozeti sabit `orange-*` yerine `brand-*` skalasına geçti. |
| 6 | Panelin geri kalanı (Favorilerim, Okuma Listem/Okuduklarım, Defterim/Notlarım/Alıntılarım, Satın Aldıklarım, Yazar'ın Yayınlarım listesi) | ✅ Tamamlandı | Hepsi Kitaplığım'da kurulan kalıba geçirildi: ham `bg-white border border-slate-200 rounded-lg` → `.card`, elle yazılmış buton class'ları → `.btn-outline`/`.btn-dark` (+ `.btn-sm` liste satırlarında), sabit `text-orange-700` içerik-türü etiketleri → `.pill-tag` mantığıyla aynı `text-brand-700` (marka rengiyle tutarlı olsun diye). Değişen dosyalar: `panel/favorilerim/_liste.blade.php`, `panel/okuma-listesi/_liste.blade.php`, `panel/notlar/_liste.blade.php`, `panel/notlar/_form.blade.php` (form kartı + "Kaydet" butonu), `panel/satin-aldiklarim/index.blade.php`, `panel/yayinlarim/_liste.blade.php`, `panel/yayinlarim/taslaklarim.blade.php` ("Yeni Taslak Oluştur" butonu), `panel/placeholder.blade.php`. **Kapsam dışı bırakıldı:** yazarın kitap/makale/bölüm **düzenleme-oluşturma formları** (`kitap-duzenle`, `kitap-bolumler`, `makale-duzenle`, `bolum-form`, `yeni`) — bunlar "liste" değil, form tasarımı ayrı bir iş. Revizyon/red notu uyarı kutuları (`bg-orange-50 border-orange-200`) bilerek dokunulmadı — kart/buton sistemi değil, semantik bir uyarı rengi. Doğrulama: `view:cache` hatasız, `npm run build` hatasız, **82/82 test yeşil**, curl ile oturum açıp her sayfa 200 döndüğü ve eski ham class'ların hiç kalmadığı teyit edildi (tarayıcı ekran görüntüsü alınamadı — bu ortamda Playwright/chromium-cli kurulu değil). |
| 7 | Abonelik sayfası | ⏳ Bekliyor | — |
| 8 | Anasayfa pilleri gerçek/tıklanabilir + `/kitaplar` katalog sayfası | ✅ Tamamlandı (2026-08-16) | Önceden "Yeni Çıkanlar" dışındaki pil'ler (Çok Satanlar/Editörün Seçkisi/Fırsatlar) sadece görsel/pasifti, anasayfa da her zaman en fazla 6 kitap gösteriyordu ("Kitaplar" kutusundaki sayı da bu 6'lık dilimin sayısını gösterip yanlış bir toplam veriyordu — 12 yayında kitap varken "6" yazıyordu). Şimdi: **`App\Enums\BookShelf`** enum'u pil mantığını tek yerden yönetiyor (`yeni`/`cok-satanlar`/`editorun-seckisi`/`firsatlar`, her biri kendi sorgusu+boş mesajıyla). `HomeController` artık `?raf=` query param'ına göre ilgili rafın ilk 6 kitabını + gerçek toplam yayında kitap sayısını gösteriyor; pil'ler artık gerçek `<a>` linki (Turbo ile SPA geçişi). "Yeni Çıkanlar" başlığının yanına **"Tümünü Gör →"** eklendi, yeni **`/kitaplar`** rotası + `BookCatalogController` + `books/index.blade.php` ile aynı 4 pil'in **tam listesi sayfalanmış** (18/sayfa) olarak görülebiliyor. "Kitaplar" stat kutusu artık `/kitaplar`'a giden bir link ve gerçek toplamı gösteriyor. **Altyapı (bilerek şimdiden kuruldu, gerçek veri gelene kadar demo ile dolduruldu):** `books` tablosuna `is_editors_pick` (boolean) ve `discount_price` (nullable decimal) sütunları eklendi (`Book` model: `scopeEditorsPick`, `scopeOnSale`, `scopeBestsellers` — bu sonuncusu **yeni sütun gerektirmedi**, var olan `purchases` ilişkisinden `withCount`+sıralama ile türetiliyor, sahte sayaç değil). `DemoContentSeeder`'a: 4 kitap `is_editors_pick=true`, 4 kitap `discount_price` dolu, ve "Çok Satanlar"ın boş görünmemesi için 6 adet demo okur hesabı (`demo.okur1..6@evrenkent.test`) + bu hesaplarla dağılımlı satın alma kayıtları eklendi — **bunlar canlıya taşınmadan önce silinmeli**, bkz. `DEPLOYMENT.md`. Fiyat gösterimi: `discount_price` doluysa kart üzerinde eski fiyat üstü çizili + indirimli fiyat gösteriliyor (hem anasayfa hem katalog kartlarında). Doğrulama: migration+seed sorunsuz, `view:cache`/`npm run build` hatasız, **82/82 test yeşil**, curl ile her `raf` değeri için doğru kart sayısı/boş durum teyit edildi. **Kapsam dışı (bu maddede):** Dergiler/Sözlükler türleri o an hâlâ "Yakında"ydı — Dergiler hemen ardından madde 9'da ele alındı, Sözlükler (veri modeli hiç yok) ayrı bir faz olarak bekliyor. |
| 9 | Dergiler genel gezinme (`/dergiler` kataloğu + sayı detay sayfası) | ✅ Tamamlandı (2026-08-16) | Mockup'ta ("1-)Evrenkent Anasayfa.png") Dergiler kutusu Kitaplar ile aynı stilde, gerçek sayıyla ("1.500+ sayı") gösteriliyordu — ama sitede genel ziyaretçiye açık **hiçbir** dergi listeleme/detay sayfası yoktu (`ArticleController` sadece tekil makale sayfasını biliyordu, bir dergi sayısının içindeki makaleleri gruplu gösteren bir sayfa hiç yoktu). Kitaplar'da kurulan kalıbın birebir aynısı dergiler için de kuruldu: `MagazineIssue::scopePublished()` eklendi; yeni `/dergiler` rotası + `MagazineCatalogController` + `magazines/index.blade.php` (yayınlanmış tüm sayıların kart görünümü, sayfalanmış); yeni `/dergiler/{magazineIssue}` rotası + `MagazineIssueController` + `magazines/show.blade.php` (sayı detayı: editör, yayın tarihi, o sayıdaki yayınlanmış makalelerin listesi — her biri `/makaleler/{slug}`'a link). Kitap kapağına eşdeğer yeni bir `x-magazine-cover` bileşeni eklendi (aynı gradyan-placeholder mantığı, gazete ikonuyla). Anasayfadaki "Dergiler" kutusu artık pasif/"Yakında" değil — `/dergiler`'e giden gerçek bir link, gerçek yayınlanmış sayı adedini gösteriyor. **Not:** `MagazineIssue`'da `slug` sütunu yok, detay sayfası `id` ile route model binding kullanıyor (Kitaplar/Makaleler `slug` kullanıyor) — tutarlılık için ileride bir `slug` sütunu eklenebilir, şimdilik pragmatik bırakıldı. **Kapsam dışı:** dergi sayısı için satın alma/abonelik mekanizması yok (mockup'ta da yok, makaleler serbestçe okunabiliyor); Sözlükler hâlâ "Yakında" (veri modeli hiç yok, ayrı bir faz); mockup'taki 5. pil "Yakında Çıkacaklar" henüz eklenmedi. Doğrulama: `view:cache`/`npm run build` hatasız, **82/82 test yeşil**, curl ile `/dergiler` ve `/dergiler/1` 200 döndüğü, anasayfadaki sayının gerçek toplamla (2) eşleştiği teyit edildi. |
| 10 | Anasayfa: tek-tür pil vitrini yerine çok raflı keşif sayfası (sonra geri alındı) | ⚠️ Denendi, geri alındı — bkz. madde 11 | Kullanıcı geri bildirimiyle "her biri kendi başlığı + Tümünü Gör'lü alt alta raflar" denendi (Yeni Çıkanlar/Çok Satanlar/Editörün Seçkisi/Fırsatlar/Dergiler, hepsi aynı sayfada). Mockup'ı ("1-)Evrenkent Anasayfa.png") tekrar incelerken bunun mockup'ın niyetiyle örtüşmediği anlaşıldı — mockup'ta Kitaplar kutusu turuncu çerçeveyle **seçili** gösteriliyor, bu net bir "tip anahtarı/tab" arayüzü. Kullanıcı onayıyla madde 11'de geri alındı. |
| 11 | Anasayfa: mockup'a sadık tip anahtarı (Kitaplar/Dergiler seçilebilir) + pil modeli | ✅ Tamamlandı (2026-08-16) | Madde 10'u geri alıp mockup'ın görsel diline dönüldü: üstteki **Kitaplar/Dergiler/Sözlükler kutuları her zaman görünür**, biri **seçili** (`?tur=kitaplar|dergiler`, varsayılan kitaplar), seçime göre altındaki pil grubu + içerik değişir. **Kitaplar seçiliyken:** 4 `BookShelf` pili (Yeni Çıkanlar/Çok Satanlar/Editörün Seçkisi/Fırsatlar) + `?raf=` ile seçilen rafın 6 kitabı + "Tümünü Gör →" `/kitaplar`'a. **Dergiler seçiliyken:** tek pil ("Yeni Sayılar" — dergide "çok satan/indirim" kavramı yok, facet'e gerek yok) + son 6 sayı + "Tümünü Gör →" `/dergiler`'e. **Sözlükler** hâlâ "Yakında" (tıklanamaz, veri modeli yok). `HomeController::index()` artık `?tur=` ve `?raf=` query param'larına göre iki ayrı veri seti hazırlıyor. Doğrulama: `view:cache`/`npm run build` hatasız, **82/82 test yeşil**, curl ile `tur=kitaplar` (varsayılan + 4 raf) ve `tur=dergiler` kombinasyonlarının hepsinde doğru kart sayısı teyit edildi. **Kapsam dışı:** mockup'taki 5. pil "Yakında Çıkacaklar" hâlâ eklenmedi. |
| 12 | Ortak kabuk: footer ekran altına yapışmıyor + sidebar rengi | ✅ Tamamlandı (2026-08-16) | Kullanıcı ekran görüntüsüyle iki sorun bildirdi (tüm sayfalarda, sadece anasayfada değil): **(a)** içerik kısa olan sayfalarda footer ekranın altına değil, içeriğin hemen altına yapışıyordu. Sebep: `layouts/public.blade.php`'de header-altı satırı saran div'de `items-start` vardı — bu, içerik kolonunun (main+footer) satırın tam yüksekliğine **germesini** engelliyor, kolonu kendi içeriği kadar kısa bırakıyordu. `items-start` kaldırıldı (varsayılan `items-stretch`); sidebar (`aside`) zaten kendi `self-start` sınıfını taşıdığı için bundan etkilenmedi, sadece içerik kolonu düzeldi — sticky footer paterni artık doğru çalışıyor. **(b)** sidebar `bg-white` (saf beyaz) kullanıyordu, `body`/`header` ise `bg-paper` (`#FBF9F4`, krem) — mockup'ta (`2.1-)...sidebar (okur için).png`) sidebar sayfanın geri kalanıyla aynı krem tonunda, ayrı bir panel gibi görünmüyor. `aside`'ın rengi `bg-paper`'a çevrildi. Doğrulama: `view:cache`/`npm run build` hatasız, **82/82 test yeşil**, curl ile ilgili class'ların doğru render olduğu teyit edildi. |
| 13 | Header tam genişlik (edge-to-edge), sidebar ile dikey hizalı | ✅ Tamamlandı (2026-08-16) | Kullanıcı ekran görüntüsüyle bildirdi: header içeriği (hamburger/logo solda, arama/sepet/çıkış sağda) ana içerikle aynı `max-w-6xl mx-auto` kutusuna hapsolmuştu, sidebar ise bu kutunun dışında gerçek ekran kenarına yapışıktı — sonuç, logo sidebar'ın sol kenarıyla hizasız, boşlukta duruyordu. `layouts/public.blade.php`'de header'ı saran `max-w-6xl mx-auto px-6` → sadece `px-6` (tam genişlik, YouTube tarzı edge-to-edge): hamburger/logo artık gerçek sol kenara, sağdaki ikonlar gerçek sağ kenara yakın, sidebar'ın sol kenarıyla dikey hizalı. Ana içerik (`@yield('content')`) hâlâ `max-w-6xl` ile ortalı, oranı değişmedi — sadece header'ın kendisi tam genişliğe yayıldı. Doğrulama: `view:cache`/`npm run build` hatasız, **82/82 test yeşil**. |
| 14 | Header: gerçek arama çubuğu (görsel) + gerçek bildirim sistemi (zil) | ✅ Tamamlandı (2026-08-16) | Mockup'ta (`2.1-)...sidebar (okur için).png`) header'da iki eksik vardı: **(a)** sadece büyüteç ikonu değil, gerçek bir arama input kutusu ("Kitap, yazar veya konu ara…"), **(b)** sepetin yanında bir bildirim zili (okunmamış işareti olan). Arama: arka planda arama motoru/sorgu olmadığı için **görsel/pasif** bırakıldı (`readonly`, `title="Yakında"`, `cursor-not-allowed`) — daha önceki "sahte veri gösterme" prensibiyle tutarlı. Bildirim: kullanıcı bunun **gerçek altyapıyla** kurulmasını istedi — Laravel'in kendi `notifications` tablosu + `Notifiable` (User modelinde zaten vardı) kullanılarak: `App\Notifications\ContentApproved` / `ContentRevisionRequested` / `ContentPublished` (ortak mantık `Concerns\DescribesContent` trait'inde — içerik türü etiketi + yazarın/editörün kendi yönetim sayfasına link). Bu bildirimler artık Filament'teki gerçek **Onayla/Reddet/Yayınla** aksiyonlarından (`BookResource`/`ArticleResource`/`MagazineIssueResource`) tetikleniyor — admin bir kitabı onayladığında yazar gerçekten bildirim alıyor (sahte/statik veri değil). Yeni `x-notifications-bell` bileşeni: header'da zil ikonu + okunmamışsa nokta rozeti, tıklayınca son 8 bildirimi listeleyen dropdown (Alpine), "Tümünü okundu işaretle" + tek tek okundu işaretleyip ilgili sayfaya giden linkler. `NotificationController@read`/`@readAll` + `panel.bildirimler.oku`/`panel.bildirimler.tumunu-oku` route'ları. Header'ın kendisi de artık üç bölgeye ayrıldı: sol (hamburger+logo), orta (arama, `flex-1 max-w-md`), sağ (`ms-auto` ile kenara itilmiş ikonlar) — `justify-between` yerine bu üçlü düzen kullanıldı. **Demo veri:** `DemoContentSeeder`'a zaten var olan revizyon/yayın kayıtlarına karşılık gelen 6 gerçek bildirim eklendi (`notifyOnce()` yardımcı metodu ile idempotent — seeder tekrar çalıştırıldığında çoğalmıyor, doğrulandı). Doğrulama: `view:cache`/`npm run build` hatasız, **82/82 test yeşil**, tinker ile bildirim gönderimi + curl ile login→zil rozeti görünüyor→"Tümünü okundu işaretle"→rozet kayboluyor akışı uçtan uca teyit edildi. |
| 15 | Filament: Editörün Seçkisi/Fırsatlar admin'den yönetilebiliyor | ✅ Tamamlandı (2026-08-16) | Madde 8'de kurulan `is_editors_pick`/`discount_price` altyapısı Filament'te **hiçbir yerde yoktu** — sadece seeder/tinker ile değiştirilebiliyordu, Süper Admin arayüzden bu iki alanı hiç yönetemiyordu (bugünkü taramada bulunan gerçek bir eksik). `BookResource` formuna `discount_price` (`TextInput`, `->lt('price')` doğrulaması — indirimli fiyat orijinal fiyattan büyük olamaz, boşsa "Fırsatlar"a girmez) ve `is_editors_pick` (`Toggle`) eklendi; tablo (liste) görünümüne de aynı iki sütun eklendi (`discount_price` para birimi formatlı, `is_editors_pick` ikon sütunu), ikisi de `->toggleable()` — varsayılan gizli değiller ama istenirse gizlenebilir. Doğrulama: **82/82 test yeşil**, Playwright ile admin girişi yapılıp `/admin/books/{id}/edit` sayfasında iki alanın da render olduğu ve zaten seed edilmiş "Editörün Seçkisi" kitabında toggle'ın doğru şekilde açık göründüğü ekran görüntüsüyle teyit edildi. |
| 16 | Bugünkü yeni özelliklere feature testleri | ✅ Tamamlandı (2026-08-16) | Madde 8-15'te eklenen hiçbir yeni davranışın (raf/tip anahtarı mantığı, `/kitaplar` ve `/dergiler` katalog sayfaları, dergi sayısı detay sayfası, bildirim gönderimi/okundu-işaretleme) otomatik testi yoktu — sadece elle curl/Playwright ile doğrulanmıştı, ileride biri bir şeyi bozarsa fark edilmezdi (bugünkü taramada bulunan bir risk). 6 yeni test dosyası eklendi: **`ContentNotificationTest`** (en kritik — Filament'teki Onayla/Reddet/Yayınla aksiyonlarının `Notification::fake()` ile gerçekten doğru kişiye/doğru veriyle bildirim gönderdiğini doğrular; dergi sayısında bildirimin admin'e değil **editöre** gittiğini de kontrol eder), **`HomeControllerTest`** (varsayılan raf, gerçek toplam sayaç, her rafın doğru filtre/sıralamayı uyguladığı — Çok Satanlar gerçekten satış sayısına göre sıralıyor mu, Fırsatlar'da indirimli fiyat gösteriliyor mu, boş raf kendi mesajını mı gösteriyor), **`BookCatalogControllerTest`** (tam liste + sayfalama 18/sayfa + geçersiz `raf` değerinde hataya düşmeden varsayılana dönme), **`MagazineCatalogControllerTest`**, **`MagazineIssueShowTest`** (yayında/taslak/editörün kendi taslağını önizlemesi — `BookShowPageTest` ile aynı kalıp), **`NotificationControllerTest`** (okundu işaretleme + **başka kullanıcının bildirimini okundu işaretleyememe** yetki sınırı). Sonuç: **108/108 test yeşil** (82 eski + 26 yeni, ilk seferde hiç hata yok). |
| 17 | Kitap Tanıtım Sayfası: mockup'a birebir yakınsama (`5-)Kitap Tanıtım Sayfası.png`) | ✅ Tamamlandı (2026-08-16) | Kullanıcı iki ekran görüntüsünü (bizim hâl + mockup) yan yana kıyaslayıp ayrıntılı bir talimat verdi. **Yeni yerleşim sırası:** başlık üstte → yazar altında (marka renginde) → Tür · Yıl · "Evrenkent Yayınları" (tek yayınevi olduğu için sabit metin, ayrı sütun değil) → **değerlendirme** (yıldız + "4.8 (128 değerlendirme)") → **içerik istatistik şeridi** (sayfa/belge/video/harita/yazar notu/kaynak — hangileri doluysa sadece onlar, artı gerçek bölüm sayısı en başta) → durum+kategori pilleri → "Kitap Hakkında" (artık `.card` kutusu içinde değil, düz metin). **Yeni altyapı:** `books` tablosuna `average_rating`, `review_count`, `page_count`, `document_count`, `video_count`, `map_count`, `author_note_count`, `source_count` eklendi; `Book::contentStats()` sadece dolu alanları döner (boş olan hiç görünmez — kullanıcının kendi ifadesiyle "hangileri varsa onlar görünür"). **Değerlendirme alanları** (average_rating/review_count) sadece Filament'te (Süper Admin) — kullanıcı bilerek "şimdilik örnek verilerle destekleyelim, canlıya alınca gerçek veriler gelir" dedi, yani bu istisnai olarak önceki "sahte veri gösterme" prensibinin **bilinçli, kullanıcı onaylı bir geçici istisnası** (gerçek yorum sistemi gelene kadar). **İçerik istatistikleri** ise hem Filament'te hem **yazarın kendi panelinde** (`panel/yayinlarim/kitap-duzenle.blade.php` + `PublicationController::updateBook`) düzenlenebiliyor — kullanıcının açık isteği "kitabı yükleyen kişi bunları panelden düzenler". Sağdaki satın alma kartına **"Sepete Ekle"** eklendi (Satın Al'ın altına) — gerçek bir sepet backend'i olmadığı için diğer sepet ikonlarıyla tutarlı şekilde görsel/pasif (`title="Yakında"`). **Güven rozeti kutusu** eklendi ("Güvenli ödeme · Anında erişim · Tüm cihazlarda oku", yeşil, sadece yayında kitaplarda). **Öneri kartları** ("Bu Eserler de Dikkatini Çekebilir") dikey düzenden mockup'taki yatay düzene (küçük kapak solda, bilgi sağda) çevrildi — sadece bu sayfaya özel, anasayfa/katalog kartları hâlâ dikey (mockup'ta da öyle). **Ayrıca düzeltildi:** `DatabaseSeeder`/`DemoContentSeeder`'daki demo yazar/editör hesaplarının adı literal olarak "Yazar"/"Dergi Editörü" idi (kitap kartlarında "YAZAR" diye görünüyordu, önceden bilinen kozmetik bir seed-veri hatasıydı) — artık "Ahmet Yılmaz"/"Ayşe Demir". Demo veri: 3 kitap farklı doluluk seviyeleriyle işaretlendi (tam dolu / kısmi / hiç yok) — "sadece doldurulan alan görünür" davranışını kanıtlasın diye. Doğrulama: `view:cache`/`npm run build` hatasız, **111/111 test yeşil** (3 yeni test: değerlendirme sadece doluysa görünüyor, istatistik şeridi sadece dolu alanları gösteriyor, yazar panelden içerik istatistiği kaydedebiliyor), Playwright ekran görüntüsüyle mockup'la görsel kıyaslama yapıldı. |
| 18 | Ortak `x-detail-header` bileşeni: tanıtım sayfaları arası tutarlı dikey ritim | ✅ Tamamlandı (2026-08-16) | Kullanıcı "dergi tanıtım sayfasında da aynı sıkışıklık sorunu çıkabilir, temeli sağlam atalım" dedi — madde 17'de kitap sayfasına özel yapılan başlık/yazar/meta/değerlendirme/istatistik dikey ritmi tek bir dosyaya (`books/show.blade.php`) gömülüydü, aynısını tekrar yazmadan dergi/makale sayfalarına taşımanın yolu yoktu. Yeni `resources/views/components/detail-header.blade.php`: başlık → kişi adı (yazar/editör) → meta satırı (dizi, `array_filter` ile boşlar elenir) → (varsa) değerlendirme → (varsa) istatistik şeridi → (slot) etiketler — hepsi tek yerde tanımlı dikey aralıklarla (`mt-2`/`mt-2`/`mt-4`/`mt-5`/`mt-5`). Üç sayfa da buna taşındı: **`books/show.blade.php`** (değerlendirme+istatistik dolu), **`magazines/show.blade.php`** (sadece byline+meta+durum rozeti — sıra da düzeltildi, önceden "Sayı N" etiketi başlığın üstünde, editör+tarih ayrı bir satırda farklı bir ritimdeydi), **`articles/show.blade.php`** (aynı şekilde taşındı; bu arada üstünde duran eski `text-orange-700` kalıntısı da düzeldi — restyle'dan kaçmış bir noktaydı). Artık üçü de gelecekte aynı görünmeye devam edecek: biri değişirse hepsi değişir. Doğrulama: `view:cache` hatasız, **111/111 test yeşil**, Playwright ile üç sayfanın ekran görüntüsü alınıp aynı ritimde olduğu gözle teyit edildi. |
| 19 | Kitap etiketleri: çoklu + tıklanabilir + kategori kataloğu, "Yayında" rozeti gereksiz yerde kaldırıldı | ✅ Tamamlandı (2026-08-16) | Kullanıcı üç şey istedi: (a) başlık/yazar/meta tipografisi bir tık büyüsün + aralıklar artsın (`x-detail-header`'da uygulandı: başlık `text-3xl/4xl`, byline `text-base`, aralıklar bir kademe daha büyütüldü), (b) herkese açık sayfada gereksiz duran **"Yayında" rozeti kaldırılsın** — kitap zaten yayındaysa bunu göstermenin anlamı yok; sadece yazar kendi taslağını/incelemedeki halini önizlerken durumu görsün diye `$book->status !== Yayinda` koşuluna bağlandı, (c) **kategori etiketleri çoğullaşıp tıklanabilir olsun**, tıklanınca aynı etikete sahip diğer kitapların toplandığı bir sayfaya gitsin. `Category` modeli (slug'lı, `belongsToMany` zaten vardı) etrafında: **yazarın kendi paneli** (`panel/yayinlarim/kitap-duzenle.blade.php` + `PublicationController::updateBook`) artık checkbox listesiyle **birden fazla kategori** seçtirip `sync()` ediyor — önceden bu alan sadece Filament'te (admin) vardı, yazar hiç kategori atayamıyordu. Kitap sayfasındaki etiketler artık `<span>` değil `<a href="/kitaplar?kategori=slug">`. `BookCatalogController` artık `?kategori=` query param'ını da destekliyor (raf'ları geçersiz kılar, o kategorideki tüm yayında kitapları sayfalanmış listeler; bilinmeyen slug 404), `books/index.blade.php` kategori bağlamında başlığı kategori adına çeviriyor + "Tüm kitaplar →" geri linki gösteriyor. `CategoryFactory` eklendi (testler için, önceden yoktu). Demo veri: "Sislerin Ardındaki Fener" artık 3 etiketli (Roman, Deneme, Tarih), "Zamansız Yolculuk" 2 etiketli — çoklu etiketin nasıl göründüğünü kanıtlasın diye. Doğrulama: `view:cache`/`npm run build` hatasız, **116/116 test yeşil** (5 yeni test: kategori filtresi doğru kitapları listeliyor mu, bilinmeyen kategori 404 mü, yazar birden fazla kategori atayıp kaldırabiliyor mu, etiketler doğru URL'e link veriyor mu, "Yayında" rozeti herkese açık sayfada gizli/yazar önizlemesinde görünür mü), curl ile `/kitaplar?kategori=deneme`'nin doğru iki kitabı listelediği teyit edildi. |
| 20 | "Yakında Çıkacaklar" 5. pil — üretime hazır altyapı | ✅ Tamamlandı (2026-08-17) | Mockup'taki 5. pil ("Yakında Çıkacaklar") hiç yoktu. Kullanıcı kararı: **yazar hedef tarih önerir, admin onaylarken kesinleştirir**; sadece tanıtım (satın alma/okuma yok); tarih gelince otomatik yayına düşer. `books` tablosuna `scheduled_publish_at` (nullable datetime) eklendi. Yazar paneli (`kitap-duzenle.blade.php` + `PublicationController::updateBook`) opsiyonel "Hedef Yayın Tarihi" alanı kazandı (`after:now` doğrulaması). Filament `BookResource`'taki **Onayla** aksiyonu artık bir `DateTimePicker` formu içeriyor — yazarın önerisiyle prefill olur, admin değiştirip onaylar (boş bırakılırsa eskisi gibi elle "Yayınla" bekler). `BookShelf::YakindaCikacaklar` yeni case'i + `Book::scopeUpcoming()` (Onaylandı + tarih dolu + gelecekte). Yeni Artisan komutu **`books:publish-scheduled`** — tarihi gelmiş kitapları otomatik "Yayında"ya çevirip yazara `ContentPublished` bildirimi yolluyor; `bootstrap/app.php`'de `withSchedule()` ile her dakika çalışacak şekilde kayıtlı (canlıda çalışması için sunucuda `php artisan schedule:run` cron'u gerekiyor, bkz. `DEPLOYMENT.md`). `BookController::show()` görünürlüğü genişledi: "Onaylandı + tarihi dolu" kitaplar artık herkese açık bir **teaser** sayfası olarak görünüyor (satın alma/okuma/favoriler/okuma-listesi blokları gizli, yerine "X tarihinde yayınlanacak" banner'ı) — tarihsiz "Onaylandı" kitaplar hâlâ sadece yazarına görünür. Doğrulama: `view:cache`/`npm run build` hatasız, **142/142 test yeşil** (yeni `UpcomingBooksTest` + `BookApprovalFlowTest`/`PublicationDraftEditTest`'e eklenen testler), `php artisan test` ile komutun geçmiş tarihli kitabı yayınlayıp bildirim gönderdiği, gelecek tarihlileri es geçtiği doğrulandı. |
| 21 | Gerçek arama (Kitap + Dergi + Makale) | ✅ Tamamlandı (2026-08-17) | Header'daki arama kutusu artık gerçek: `GET /arama?q=` → `SearchController` — başlık/yazar-editör adı/açıklama-içerik üzerinde LIKE tabanlı arama (Scout/Meilisearch kurulu değil, veri boyutu için şimdilik gerek yok — bkz. `DEPLOYMENT.md`). Wildcard karakterleri (`%`, `_`) `addcslashes` ile escape ediliyor, kullanıcının aramasında bu karakterler beklenmedik eşleşme yapmıyor. Sonuç sayfası (`search/index.blade.php`) tür bazlı gruplu (Kitaplar/Dergiler/Makaleler kart/liste grid'leri, mevcut `x-book-cover`/`x-magazine-cover` bileşenleri yeniden kullanıldı), boş sorgu ve sonuçsuz sorgu için ayrı dürüst mesajlar var. `Article` modeline tutarlılık için `scopePublished()` eklendi (Book/MagazineIssue'da zaten vardı). Doğrulama: `view:cache`/`npm run build` hatasız, **142/142 test yeşil** (yeni `SearchTest` — başlık/yazar/içerik eşleşmesi her tür için, taslak içeriğin dönmediği, wildcard escape, boş/sonuçsuz sorgu). |
| 22 | Gerçek Sepet — çoklu ürün, mock checkout | ✅ Tamamlandı (2026-08-17) | Header'daki pasif sepet ikonu artık gerçek: girişli kullanıcıda gerçek sayı rozetiyle `/panel/sepetim`'e link, ziyaretçide `login`'e link. Yeni `cart_items` tablosu + `CartItem` modeli, yeni `CartController` (`panel.sepetim`/`panel.sepet.kitap.ekle`/`panel.sepet.kitap.sil`/`panel.sepet.checkout`) — sadece yayındaki kitaplar eklenebiliyor, zaten satın alınmış kitap tekrar eklenemiyor. Route'lar `{book}` bazlı (id-bazlı `{cartItem}` değil) — sepet her zaman `auth()->user()->cartItems()` üzerinden scope'landığı için ayrı bir Policy gerekmedi (`favoriler.kitap.toggle` ile aynı desen). **Satın alma mantığı tekilleştirildi:** `User::purchase(Book $book)` artık hem tekil "Satın Al" (`PurchaseController`) hem sepet checkout'u (`CartController`) tarafından kullanılıyor — bu arada indirimli fiyatın (varsa) doğru kaydedildiği de düzeltildi (öncesinde `PurchaseController` her zaman tam fiyatı kaydediyordu, indirim yoksayılıyordu). Checkout hâlâ **mock ödeme** — gerçek bir gateway (Stripe/iyzico) yok, bu zaten önceden belgeli bir karar (bkz. `DEPLOYMENT.md`). `books/show.blade.php`'deki pasif "Sepete Ekle" butonu gerçek forma döndü (zaten sepetteyse "Sepette" linkine dönüşüyor). `panel-nav.blade.php`'ye "Sepetim" linki eklendi. Doğrulama: `view:cache`/`npm run build` hatasız, **142/142 test yeşil** (yeni `CartTest` — sadece yayında kitap eklenebiliyor, tekrar ekleme idempotent, satın alınmış kitap eklenemiyor, silme, çoklu-ürün checkout, indirimli fiyat, guest erişemiyor). |
| 23 | Site genelinde mobil uyumluluk geçişi | ✅ Tamamlandı (2026-08-17) | Kullanıcı "genel olarak mobil uyumlu değil" dedi, tüm `resources/views` iki keşif ajanıyla tarandı. **En kritik bulgu:** ortak kabuktaki sidebar `w-72` sabit + push-tabanlı + varsayılan açıktı — 375px'te ekranın büyük kısmını kaplıyordu. Çözüm: `Alpine.store('ui').sidebarOpen` başlangıç değeri artık `window.matchMedia('(min-width:1024px)')` ile ekran genişliğine göre belirleniyor (masaüstü açık, mobil kapalı); mobilde `<aside>` `fixed` + `translate-x` ile kayan bir **overlay drawer**'a döndü (backdrop'lu, tıklayınca/menü linkine tıklayınca kapanıyor), `lg:` ve üstünde eski push davranışı birebir korundu. Header sağ taraf mobilde daraldı ("Sepetim" metni `hidden sm:inline`, ikon+rozet her zaman görünür). **Ayrıca bir kendi hatamı düzelttim:** madde 21'den sonra "arama sayfasındaki tekrar eden ikinci kutuyu kaldır" isteğini uygularken sadece masaüstünü düşünmüştüm — header'daki arama kutusu zaten mobilde gizli olduğu için mobilde arama yapmanın hiçbir yolu kalmamıştı; `/arama` sayfasına `sm:hidden` bir mobil-özel input geri eklendi. Son olarak, tüm proje genelinde tekrarlayan bir **"başlık + buton(lar)" taşma deseni** (`flex items-center justify-between`, `min-w-0`/`truncate`/`flex-wrap` güvenlik ağı yok) ~10 dosyada düzeltildi: favorilerim/okuma-listesi/yayınlarım liste satırları (bazılarında aynı anda 2-3 buton oluşabiliyordu), yayınlarım başlık satırları, satın-aldıklarım, notlar formu (sabit `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`), auth sayfaları (login/register/verify-email — `x-guest-layout`'un kendi `px-8` dolgusu yüzünden en dar nokta buradaydı). Doğrulama: `view:cache`/`npm run build` hatasız, **144/144 test yeşil**, Playwright ile 375×800 ve 1280×800 viewport'ta gerçek tarayıcı ekran görüntüsü alınıp (misafir/girişli anasayfa, sidebar açık/kapalı, arama, sepet) masaüstü davranışının hiç değişmediği, mobilde taşma olmadığı gözle teyit edildi. Bu geçişin ardından kullanıcı geri bildirimiyle küçük bir düzeltme turu daha yapıldı (ayrı madde açılmadı, commit mesajlarında kayıtlı): sepet rozetinin ikon köşesine doğru sabitlenmesi, "Sepete Git" linkinin gerçekten sepet sayfasına gitmesi, header ikonlarının (zil/arama) dikey hizası, mobilde header sağ ikonlarının CSS Grid'in `display:none` davranışı yüzünden sağa yaslanmaması (`justify-self-end` ile düzeltildi), "Çıkış Yap"ın header'dan sidebar'ın altına (kırmızı, ayrık) taşınması, sidebar scroll çubuğunun YouTube tarzı hover'da görünür hale getirilmesi. |
| 24 | Dergi Editörü dashboard'u — mockup'a gerçek veriyle yakınsama | ✅ Tamamlandı (2026-08-17) | Kullanıcı yeni özellik yerine önce yarım bırakılan işleri bitirmek istedi; `2.3-)...` (Dergi Editörü) ve `2.4-)...` (Süper Admin) mockup'ları incelendi. Süper Admin dashboard'u premium/abonelik, gelir paylaşımı, okuma analitiği, canlı akış, sunucu sağlığı gibi **hiç var olmayan altyapılar** istediği için kullanıcı kararıyla ayrı/sonraki bir işe bırakıldı — sadece Dergi Editörü ele alındı. **Kapsam kararı:** yeni dashboard gerçek verili bir **özet/genel bakış**, mutasyon gerektiren aksiyonlar (sayı oluştur, makale incele, onaya gönder/onayla/reddet/yayınla) hâlâ Filament'te (`MagazineIssueResource`/`ArticleResource`) — dashboard'daki butonlar oraya link veriyor, onay/red/bildirim mantığı iki yerde tekrar yazılmadı. Yeni `app/Http/Controllers/DergiYonetimiController.php` + `panel.dergi.*` route'ları (`role:dergi_editoru` middleware, yazarın `role:yazar` deseniyle birebir tutarlı): **Ana Sayfa** (Aktif Sayı — editörün yayında olmayan en son güncellenen sayısı; gerçek 5 maddelik checklist: Kapak/Editör Yazısı (yeni `editor_note` alanı)/Makaleler/İnceleme/Onaya Gönderildi, her biri gerçek bir alan/duruma bağlı — mockup'taki "Kaynakça"/"Hakem Kontrolü" gibi karşılığı olmayan maddeler bilerek atlandı; ilerleme %'si checklist'ten hesaplanıyor), **Sayılarım** (durum filtreli tam liste), **Makale Havuzu** (mockup'taki 5 sekme — Tümü/İncelenmeyi Bekleyen/Revizyon İstenen/Kabul Edilen/Reddedilen — `ContentStatus` gruplarına eşleniyor), **Yayın Takvimi** (gerçek sayılar, sahte "boş ay" satırı yok). Tüm sorgular `editor_id`'ye sıkı sıkıya bağlı (başka editörün verisi hiç görünmez). `panel-nav.blade.php`'ye yazarınkiyle simetrik "Dergi Yönetimi" bloğu eklendi; `User::redirectPath()` artık dergi editörünü `/admin`'e değil `/panel/dergi`'ye yönlendiriyor (yazarın kendi hub'ına yönlenmesiyle tutarlı, Filament hâlâ erişilebilir ama zorunlu ilk durak değil). Doğrulama: `view:cache`/`npm run build` hatasız, **153/153 test yeşil** (9 yeni: dashboard izolasyonu — başka editörün verisi görünmüyor, checklist gerçek veriye göre değişiyor, makale havuzu sekme filtreleri, yazar/okur 403 alıyor), Playwright ile `editor@evrenkent.test` girişiyle hem 1280px hem 375px ekran görüntüsü alınıp mockup'la görsel kıyaslama + mobilde taşma kontrolü yapıldı. |
| 25 | Dergi Editörü: Sayı Oluştur/Düzenle + Makale İnceleme akışı Filament'ten panele taşındı | ✅ Tamamlandı (2026-08-17) | Madde 24'te dashboard gerçek veriyle kuruldu ama "Sayı Oluşturucuya Git"/makale "Görüntüle" linkleri hâlâ Filament'in koyu tema varsayılan arayüzüne gidiyordu. Kullanıcı ekran görüntüsüyle gösterip "modern panelimizde olsun, burayı kullanmamaya çalışalım" dedi. Policy'ler (`MagazineIssuePolicy`/`ArticlePolicy`) zaten dergi editörünün **kendi** sayısını oluşturma/düzenleme/onaya gönderme ve **kendi** sayısındaki makaleyi inceleme yetkisini net tanımlıyordu — sadece Süper Admin'in onayla/reddet/yayınla aksiyonları policy'de zaten ona özeldi, o yüzden bu ikisi ayrımı bozmadan taşınabildi. `DergiYonetimiController`'a 6 yeni metod: `yeniSayiForm`/`storeSayi`/`sayiDuzenleForm`/`updateSayi` (kapak görseli dahil, Filament'in `FileUpload`'ıyla aynı disk/dizine yazıyor) `gonderSayi` (PublicationController::submitBook ile aynı doğrudan-`reviews()->create()` deseni), `makaleGoster`/`inceleMakale`. Yeni view'lar `.card`/`.btn-*` tasarım sistemiyle: `sayi-form.blade.php` (oluştur+düzenle ortak form), `makale-goster.blade.php` (`articles/show.blade.php`'nin aynısı + `@can('review')` ile "İncele ve Onaya Gönder" butonu). `x-article-pool-table`'daki "Görüntüle" artık kendi panelimize gidiyor, yanına `@can('review')` ile tek-tık "İncele" hızlı aksiyonu eklendi. Dashboard/Sayılarım'daki tüm Filament linkleri kaldırıldı — dergi editörünün kendi akışında artık **hiç Filament linki yok**, sadece Süper Admin'in aksiyonları orada kalıyor. Doğrulama: `npm run build`/`view:cache` hatasız, **161/161 test yeşil** (8 yeni: kapak dosyalı sayı oluşturma, sadece Taslak/RevizyonIstendi + kendi sayısını düzenleyebilme, onaya gönderme, makaleyi herkes görebiliyor ama sadece kendi sayısındaki Gönderildi makaleyi inceleyebilme), Playwright ile uçtan uca gerçek akış (sayı oluştur → düzenle sayfasına yönlen → onaya gönder → Makale Havuzu'ndan görüntüle) tarayıcıda denendi. |
| 26 | Süper Admin dashboard'u — mockup'a gerçek veriyle yakınsama | ✅ Tamamlandı (2026-08-18) | Sıra Süper Admin'e geldi; kullanıcı görsel olarak **mockup'a (`2.4-)...png`) birebir yakın** olmasını istedi — bu yüzden Filament'in kendi temasını zorlamak yerine Dergi Editörü'nde kurulan desenle aynı şekilde, Filament dışında **ayrı bir "modern panel"** (`layouts/admin-panel.blade.php`, koyu lacivert sidebar/topbar) kuruldu. Yeni `SuperAdminController` sadece **Ana Sayfa**'yı dolduruyor (kullanıcının açık kapsam kararı: diğer tüm sol menü kalemleri "Yakında" placeholder). Gerçek veriye eşlenenler: Toplam Kitap/Dergi/Kullanıcı, **Premium Üye** (`users.is_premium` — meğer zaten gerçek bir sütunmuş, Filament'te toggle'lanabiliyor, mockup'taki gibi tutulabildi), Bugünkü Satış/Gelir (`Purchase`), Bugünkü Durum (5 gerçek sayaç), Genel İstatistik Özeti (son 30 gün satış+gelir, bağımlılık eklemeden elle yazılmış inline SVG polyline — projede hiç chart kütüphanesi yok), En Çok Satan Kitaplar (`Purchase` gruplanmış), **Canlı Akış** (`ContentReview`+`Purchase`+yeni `User` kayıtları `created_at`'e göre birleştirilip kronolojik gerçek bir olay akışı — hiçbiri uydurma değil), Bekleyen Onaylar (gerçek sayaç, ilgili Filament listesine link). Altyapısı hiç olmayanlar (Gelir Payı/Premium Sistemi/Abonelikler/Sözlükler/Platform Sağlığı) **tamamen çıkarıldı**, sahte veri üretilmedi. Sidebar'daki (`x-admin-nav`) her kalem iki türden biri: zaten Filament'te çalışan bir liste ise (Kitaplar/Dergiler/Kategoriler/Kullanıcılar) doğrudan oraya link; hiç altyapısı yoksa (`SuperAdminController::placeholder`) tek bir dürüst "yakında" sayfasına link. **Bulunan gerçek bir hata:** Turbo Drive, Filament'e giden linkleri kendi body-only-swap SPA akışına dahil etmeye çalışınca Filament'in kendi Livewire/Alpine JS'i (ayrı `<head>` varlıklarına bağımlı) bozuk bir ortamda çalışıp konsola onlarca hata basıyordu (`$store.sidebar` tanımsız, çift Alpine instance'ı) — tüm Filament'e giden linklere `data-turbo="false"` eklenerek tam sayfa yüklemeye zorlandı, Playwright ile konsol hatası kalmadığı doğrulandı. `User::redirectPath()` artık `super_admin`'i `/admin` yerine `/panel/admin-panel`'e yönlendiriyor (Dergi Editörü'yle tutarlı). Doğrulama: `npm run build`/`view:clear` hatasız, **169/169 test yeşil** (7 yeni: rol yetkisi, gerçek toplamlar, bekleyen onay sayacı, canlı akışın kronolojik sırası, placeholder 200/404, redirect yolu), Playwright ile `admin@evrenkent.test` girişiyle hem 1280px hem 375px'te taşma kontrolü + Filament linklerinin konsol hatasız açıldığı teyit edildi. |
| 27 | Süper Admin: İçerik Onayları Filament'ten panele taşındı (Faz 1/N) | ✅ Tamamlandı (2026-08-19) | Kullanıcı madde 26'daki dashboard'u inceleyip "bazı bölümlerde hâlâ Filament paneline yönlendiriyor, bence oraya yönlendirme olmasın" dedi; "hepsini taşıyalım ama adım adım yapalım" diye netleşti. Bu faz sadece **içerik onay akışını** taşıyor — süper adminin en sık yaptığı iş (Kitap/Dergi Sayısı/Makale onayla/reddet/yayınla). Kitaplar/Dergiler tam listeleri, Kullanıcı/Rol yönetimi, Kategoriler **hâlâ Filament'e gidiyor** (`data-turbo="false"` ile) — bunlar sıradaki fazlarda taşınacak. Yeni `ContentApprovalController` + `/panel/admin-panel/onaylar` — Filament'teki `BookResource`/`ArticleResource`/`MagazineIssueResource`'daki `approve`/`reject`/`publish` action'larının **birebir aynısı** (aynı policy'ler, aynı 3 adım: durum güncelle + `ContentReview` kaydı + `ContentApproved`/`ContentRevisionRequested`/`ContentPublished` bildirimi) — Filament resource'ları silinmedi, bu paralel bir arayüz. 3 sekmeli liste (`?tur=kitaplar|dergiler|makaleler`), her satırda durum rozeti + "Görüntüle" (gerçek public show sayfasına — bkz. altı) + duruma göre Onayla/Reddet/Yayınla. Kitabın approve'unda opsiyonel `scheduled_publish_at` alanı olduğu için Onayla ayrı bir form sayfası (`onayla.blade.php`, tipten bağımsız tek şablon); Reddet her zaman zorunlu not istediği için o da form (`reddet.blade.php`); Yayınla tek-tık POST (projede zaten hiçbir yerde JS confirm dialog yok, "Gönder"/"İncele" gibi tek-tık desenle tutarlı). **Yan değişiklik:** `Book`/`Article`/`MagazineIssueController::show` artık `super_admin`'e onaydan önce içeriği önizleme izni veriyor (`|| $user->hasRole('super_admin')`) — mevcut public show şablonları hiç değişmeden yeniden kullanıldı, ayrı bir "admin preview" şablonu yazılmadı. Küçük ek düzeltme: `MagazineIssueController::show`'daki makale listesi artık sahibi/admin önizlerken taslak makaleleri de gösteriyor (önceden her zaman sadece Yayında filtresi vardı, bu da onay bekleyen bir sayıyı önizlerken hep boş gösteriyordu). Nav'daki "Onay Bekleyenler" ve dashboard'daki "Bekleyen Onaylar" kartının 3 linki artık kendi panelimize gidiyor, `data-turbo="false"` kaldırıldı. Doğrulama: `npm run build`/`view:clear` hatasız, **179/179 test yeşil** (10 yeni: yetki, sekme filtreleme, onayla/reddet/yayınla üç tip için de, yanlış durumdan aksiyon 403, süper admin önizleme izni), Playwright ile gerçek onayla/reddet akışı uçtan uca denendi (durum DB'de gerçekten değişti, flash mesaj göründü, badge sayısı güncellendi), 375px'te taşma yok. |

## Demo veri

- **2026-08-13:** `DemoContentSeeder`'a 13 yeni kitap eklendi (toplam 20 kitap,
  12'si "Yayında"), `php artisan db:seed --class=DemoContentSeeder` ile
  yerel DB'ye işlendi — anasayfa/kitaplığım gibi listeleme sayfaları artık
  daha gerçekçi görünüyor (2 kitap yerine 6+ kitap gridi).
  Seeder idempotent (`firstOrCreate` ile slug bazlı) — tekrar çalıştırmak
  güvenli, var olanları çoğaltmaz.
- **Not (bu fazın kapsamı dışı, ileride bakılabilir):** Temel `DatabaseSeeder`
  (proje kurulumundan beri var, benim eklemem değil) `author@evrenkent.test`
  hesabının adını literal olarak **"Yazar"** koymuş — bu yüzden bazı kitap
  kartlarında yazar adı olarak "YAZAR" görünüyor. Kozmetik bir seed-data
  detayı, gerçek bir isimle değiştirilmesi istenirse `DatabaseSeeder.php`'de.

## Değişen dosyalar (kümülatif)

- `tailwind.config.js` — `brand` renk skalası eklendi
- `resources/css/app.css` — bileşen sınıfları (`@layer components`) eklendi
- `resources/views/layouts/public.blade.php` — header restyle
- `resources/views/home.blade.php` — kategori sekmeleri, kart stilleri
- `app/Http/Controllers/BookController.php` — `categories` eager-load, `chapterCount`, `relatedBooks`
- `resources/views/books/show.blade.php` — sticky satın alma kartı, kategori etiketleri, öneri şeridi
- `resources/views/components/panel-nav.blade.php` — panel menüsü (yeni, `layouts/panel.blade.php`'den çıkarıldı)
- `resources/views/layouts/panel.blade.php` — sol menü artık `<x-panel-nav />` kullanıyor
- `resources/js/app.js` — Alpine.js zaten kuruluydu (Breeze ile geldi), panel menüsü için ek paket gerekmedi
- `database/seeders/DemoContentSeeder.php` — 13 yeni kitap örneği
- `resources/views/panel/kitapligim.blade.php` — ham Tailwind yerine `.card`/`.pill-tag`/`.btn-*` sınıflarına geçiş

## Her adımdan sonra yapılan doğrulama

1. `npm run build` — Vite derlemesi hatasız
2. `php artisan view:cache` — tüm Blade şablonları syntax hatasız derleniyor (sonra `view:clear` ile geri alınıyor, dev ortamı bozulmasın diye)
3. Gerçek sunucuda (`php artisan serve`) ekran görüntüsü alınıp mockup ile karşılaştırılıyor
